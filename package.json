{
    "name": "imagetech-prova",
    "version": "1.0.0",
    "description": "Projeto prova imagetech",
    "scripts": {
        "start": "concurrently \"cd frontend && npm start\" \"cd back-end && npm run start:dev\"",
        "install": "cd frontend && npm install && cd ../back-end && npm install && npx prisma generate && npx prisma migrate dev "
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "dependencies": {
        "@nestjs/jwt": "^10.1.1",
        "@nestjs/passport": "^10.0.1",
        "@types/bcrypt": "^5.0.0",
        "class-validator": "^0.14.0",
        "concurrently": "^8.2.1",
        "passport": "^0.6.0",
        "passport-jwt": "^4.0.1"
    }
}
